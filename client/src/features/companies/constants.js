// Matches the tags actually used in scripts/seedCompanies.js — a
// sensible default filter list, not an enforced/exhaustive set (same
// convention as dsa-tracker's TOPIC_OPTIONS).
export const TAG_OPTIONS = ["product-based", "dream-company", "mnc", "core", "fintech", "hardware"];