import { Cpu, Database, Network, Box, Table } from "lucide-react";

// Maps the icon name string stored on each Subject document (set by
// the seed script) to an actual lucide-react component.
export const SUBJECT_ICON_MAP = {
  Cpu,
  Database,
  Network,
  Box,
  Table,
};